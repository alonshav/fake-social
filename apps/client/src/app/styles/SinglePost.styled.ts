import { FlexContainer } from './Containers/FlexContainer';
import styled from 'styled-components';

export const SinglePostStyled = styled(FlexContainer)`
  width: 100%;
  .single-post{
    width: 50%;
    min-width: 500px;
  }

`
