import styled from 'styled-components';
import { StyledRoundCornerButton } from './StyledRoundCornerButton';

export const StyledLoginButton = styled(StyledRoundCornerButton)`
  &:hover{
    cursor: pointer;
  }
`
