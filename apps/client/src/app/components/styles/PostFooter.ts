import styled from 'styled-components';
import { CardFooter } from './CardFooter.styled';

export const PostFooter = styled(CardFooter)`
  span{
    display: flex;
    align-items: center;
    font-size: 10px;
    font-weight: 100;
  }

`
